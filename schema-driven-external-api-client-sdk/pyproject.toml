[project]
name = "schema-driven-external-api-client-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy>=1.20.0",
    "pytest>=9.0.2",
]
